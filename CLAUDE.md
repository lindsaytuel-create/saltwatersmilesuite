# Saltwater Smile Suite — Project Instructions

## Git
- Do NOT push to the repo unless the user explicitly says "push to repo" or "push."
- Make changes locally and wait for push instructions.
- When the user says "push," it means the full flow: `git add` the changed files, commit with a descriptive message, then `git push`. Don't just run `git push` on its own.
